apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/component: webhook
    app.kubernetes.io/name: instana
    app.kubernetes.io/version: 237-3
  name: instana-operator-webhook
  namespace: instana-units
spec:
  dnsNames:
    - instana-operator-webhook.instana-units
    - instana-operator-webhook.instana-units.svc
  issuerRef:
    kind: Issuer
    name: instana-operator-webhook
  secretName: instana-operator-webhook
  subject:
    organizations:
      - instana
