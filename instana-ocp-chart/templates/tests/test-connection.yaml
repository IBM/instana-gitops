apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "instana-ocp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "instana-ocp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "instana-ocp-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
