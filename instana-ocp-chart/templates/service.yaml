apiVersion: v1
kind: Service
metadata:
  name: {{ include "instana-ocp-chart.fullname" . }}
  labels:
    {{- include "instana-ocp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "instana-ocp-chart.selectorLabels" . | nindent 4 }}
